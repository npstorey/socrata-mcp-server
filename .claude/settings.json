{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "sandbox": {
    "_shakedown": "Measured 2026-08-22, fresh top-level session, enabled:true via settings.local.json (denyRead ~/code/civic-ai-ops/, allowWrite ~/.npm) overriding this file's enabled:false. This supersedes the prior note: the sandbox DID initialize, and the EPERM srt-mux socket failure it records did not recur. FILESYSTEM LAYER ENFORCING (two probes): `ls /Users/npstorey/.claude/bridge-spawn` returned `Operation not permitted`, and a write to `/Users/npstorey/.sandbox-write-probe-<pid>`, outside allowWrite, returned `operation not permitted`. A third, unplanned probe: writing THIS file from Bash was itself denied — `(eval):1: operation not permitted: .claude/settings.json` — so settings files are write-protected from the shell and must be edited through the Edit tool. NETWORK IN PROMPT MODE, NOT DENY (three hosts): example.com 200, www.wikipedia.org 200, registry.npmjs.org 200 — response bodies were fetched and are real upstream content, not a proxy block page. So a 200 from a non-allowlisted host is NOT evidence the sandbox is off; the prior note drew exactly that inference and it was wrong. `network.strictAllowlist: true` is the switch that denies instead of prompting: it is set below, is UNMEASURED in this session, and takes effect from the next one. `ls ~/code/civic-ai-ops` never ran — refused at the permission layer, not by the sandbox — so the denyRead itself remains untested. No Node fetch ENOTFOUND (proxy-unawareness, not the allowlist) was observed. GATES, after a clean `npm ci` of 326 packages: (1) `npm run clean && npm run build:tsc` PASS, silent, exit 0. (2) `npm test` FAIL, and the failure is SANDBOX-CAUSED — verbatim `Error: listen EPERM: operation not permitted 0.0.0.0`. supertest binds an ephemeral port and the sandbox denies the listen, giving `Test Files 1 failed | 14 passed | 2 skipped (17)` and `Tests 4 failed | 88 passed | 9 skipped (101)` against the documented 92 passed; all four failures are in src/__tests__/openai-initialize.test.ts and the delta is exactly the four tests that bind. (3) `npm run lint` PASS, `✖ 117 problems (0 errors, 117 warnings)`, exit 0. Hence enabled stays FALSE: under this config `npm test` cannot pass in this repo, so committing enabled:true would red-gate every session here. To flip it true, first permit a loopback bind for the supertest server, then re-run the three gates. Unrelated friction, not gate-blocking: `eval $(fnm env) && fnm use 22` fails with `Can't create the symlink for multishells at /Users/npstorey/.local/state/fnm_multishells/...: Operation not permitted`, but the profile-initialized PATH already provides Node v22.23.1, matching .node-version, so no workaround was needed.",
    "enabled": false,
    "allowUnsandboxedCommands": false,
    "excludedCommands": ["gh *", "git *"],
    "filesystem": {
      "allowWrite": ["~/.npm"]
    },
    "network": {
      "strictAllowlist": true,
      "allowedDomains": [
        "registry.npmjs.org",
        "github.com",
        "api.github.com",
        "objects.githubusercontent.com",
        "codeload.github.com",
        "data.cityofnewyork.us"
      ]
    }
  }
}
